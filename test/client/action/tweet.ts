import * as assert from 'power-assert';
import * as action from '../../../client/action/tweet';
import { TweetModel } from '../../../client/model/tweet';

// describe('tweetAction', () => {
//   it('postTwet', () => {
//     const status = new TweetModel({ text: 'hoge' });
//     return action.postTweet('2979592160', status).then(val => assert(val));
//   })
// })